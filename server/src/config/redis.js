// Day 2 placeholder for Redis Pub/Sub. Keeping the file in place now makes the
// backend structure ready for horizontal scaling without changing import paths later.
module.exports = {
  pubClient: null,
  subClient: null,
};
